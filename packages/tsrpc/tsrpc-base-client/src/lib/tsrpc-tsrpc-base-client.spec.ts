import { tsrpcTsrpcBaseClient } from './tsrpc-tsrpc-base-client';

describe('tsrpcTsrpcBaseClient', () => {
  it('should work', () => {
    expect(tsrpcTsrpcBaseClient()).toEqual('tsrpc-tsrpc-base-client');
  });
});
